# Stage 2: same pipeline, harder negatives + longer spacing
base: tum_stage1.yaml

run:
  name: tum_stage2_dinov3_harder_pairs_v1

dataset:
  frame_spacing_min: 2
  frame_spacing_max: 6

training:
  epochs: 10
  lr: 1.0e-4

loss:
  contrastive:
    max_positives: 768
  detector:
    weight: 0.7
