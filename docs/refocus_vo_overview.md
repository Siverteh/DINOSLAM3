# Dense Refocus Layout

- `src/refocus_vo/`: canonical Python package for dense refocus VO
- `scripts/`: canonical shell entrypoints
- `runs/`: canonical output root for new dense-refocus runs

This subtree isolates dense VO work from the large legacy `runs/` tree and the sparse training pipeline.
